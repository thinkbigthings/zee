
jar {
	baseName = 'zee-engine-samples'
	version = '0.9.4-SNAPSHOT'
}

dependencies {
	compile project(':engine')
}

// don't generate javadocs for example project
tasks.withType(Javadoc).all { enabled = false }
